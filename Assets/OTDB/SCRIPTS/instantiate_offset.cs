using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class Misiones : MonoBehaviour
{
    /*public GameObject particlesFire;
    public GameObject[] offsetFire;
    public List<Vector3> targetPositions;
    private float timeSpawn = 5f;
    private int randomOffset;
    
    void Start()
    {
        StartCoroutine("SpawnRandomTarget");
    }

    private IEnumerator SpawnRandomTarget()
    {
        //CORRUTINA
        while (!gameOver)
        {
            //TIEMPO DE REPETICION
            yield return new WaitForSeconds(timeSpawn);
            //LUGAR ALEATORIO
            randomOffset = Random.Range(0, targetPrefabs.Length);

            while (targetPositions.Contains(randomOffset))
            { ranSpawnPos = RandomSpawnPosition(); }
            //INSTANCIAR
            Instantiate(particlesFire, offsetFire[randomOffset].transform.position, particlesFire.transform.rotation);
            targetPositions.Add(randomOffset);
        }
    }
    */
}
