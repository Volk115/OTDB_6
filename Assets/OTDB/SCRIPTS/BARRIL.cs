using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BARRIL : MonoBehaviour
{
    /*
    public DETECT_COLISIONS playerControllerScript;

    public ParticleSystem PARTICLE_AGUA;

    void Start()
    {
        playerControllerScript = FindObjectOfType<DETECT_COLISIONS>();

    }

    void Update()
    {
        if (playerControllerScript)
        {
            Instantiate(PARTICLE_AGUA, transform.position, gameObject.transform.rotation);
        }
        
    }
    */

}
